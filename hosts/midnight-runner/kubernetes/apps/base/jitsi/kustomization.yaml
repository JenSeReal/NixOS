---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: jitsi
resources:
  - helm-release.yaml
  - httproute.yaml
  - jitsi-secrets.sops.yaml
  - namespace.yaml
components:
  - ../../../components/helm-configmap-reference
configMapGenerator:
  - name: values
    files:
      - values.yaml=values.yaml
