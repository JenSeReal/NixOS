---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
configMapGenerator:
  - name: blueprints
    options:
      disableNameSuffixHash: true
      labels:
        blueprints.goauthentik.io/instantiate: "true"
    files:
      - whoami-blueprint.yaml
      - jitsi-blueprint.yaml
